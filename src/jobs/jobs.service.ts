import { Injectable } from '@nestjs/common';

@Injectable()
export class JobsService{

    findAll(){

    }

    find(id: number){
        return id;
    }

    create() {

    }

    edit(id: number) {
        return id;
    }

    remove(id: number) {
        return id;
    }
}
